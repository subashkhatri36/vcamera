import 'package:flutter/material.dart';

class LandScapeHomeCamera extends StatelessWidget {
  const LandScapeHomeCamera({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
